// Vercel requires a default export named `middleware` from this file.
// The implementation lives in proxy.ts; this file is the required entry point.
export { proxy as default, config } from './proxy';
